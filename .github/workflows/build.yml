name: Build IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3

      - run: chmod +x ipabuild.sh

      - run: ./ipabuild.sh

      - uses: actions/upload-artifact@v4
        with:
          name: ipa
          path: '**/*.ipa'
